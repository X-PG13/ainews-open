# AI News Open v1.2.51

## Highlights

- Added a maintainer dependency update policy that keeps Dependabot PRs as upgrade notifications rather than directly mergeable bot-authored commits.
- Documented the human-authored review branch flow for dependency and GitHub Actions upgrades in English and Simplified Chinese.
- Clarified that the original Dependabot PR should be closed after the human-authored upgrade PR merges.

## Deferred

- PyPI trusted publishing remains intentionally deferred in the `Deferred: PyPI` milestone until the PyPI project and trusted publisher are configured.

## Validation

- `make check PYTHON=./.venv-dev/bin/python`
- `Release workflow on tag push`
