# AI News Open v1.2.45

## Highlights

- Added digest-editor `suppress` controls so maintainers can exclude articles from digest selection without hiding them globally.
- Added a digest preview route and dashboard panel that shows `selected`, `suppressed`, `duplicate_secondary`, and `ranked_out` decisions with explicit reasons.
- Extended cross-source curation so duplicate-cluster primary switching and suppress state flow through API responses and the admin console.

## Validation

- `make check PYTHON=./.venv-dev/bin/python`
- `node --check src/ainews/web/app.js`
- `Release Artifact Smoke`
