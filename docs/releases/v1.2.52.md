# AI News Open v1.2.52

## Highlights

- Added a release metadata regression test so release prep fails when package metadata, runtime version, changelog, release notes, or release indexes drift apart.
- Added a manual dependency and GitHub Actions update checklist that keeps Dependabot PRs as notifications and requires maintainer-authored review branches for upgrades.
- Expanded the security policy with private disclosure expectations, maintainer triage, fix and release handling, and public disclosure guidance.
- Linked the security reporting and response process from README, support policy, and support lifecycle docs.

## Deferred

- PyPI trusted publishing remains intentionally deferred in the `Deferred: PyPI` milestone until the PyPI project and trusted publisher are configured.

## Validation

- `make check PYTHON=./.venv-dev/bin/python`
- `Release workflow on tag push`
