# AI News Open v1.2.68

## Highlights

- Fixed console rendering in local file-mode previews by switching static asset references in the admin UI to relative paths.
- Added visibility for the latest successful data refresh in the console operator status strip (`上次同步`).
- Added troubleshooting and local-preview guidance in README and troubleshooting docs for cases where the console shows unstyled plain text.

## Deferred

- PyPI trusted publishing remains intentionally deferred in the `Deferred: PyPI` milestone until the project and trusted publisher are configured.

## Validation

- `make check PYTHON=./.venv-dev/bin/python`
- Confirmed release metadata checks and documentation link checks pass before opening the release PR.
