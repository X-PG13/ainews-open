# AI News Open v1.2.53

## Highlights

- Quieted local smoke startup retries so healthy checks no longer print expected `curl: (7)` connection noise while the server is still starting.
- Added a release asset naming regression guard that keeps the wheel, source archive, `sha256sums.txt`, and SBOM filenames aligned across package metadata, release docs, and workflows.
- Tightened release artifact smoke to download exact expected asset names, including the SBOM, instead of relying on broad artifact globs.
- Added maintainer recovery notes for interrupted GitHub PR merge, tag fetch, release asset verification, and network timeout flows in English and Simplified Chinese.

## Deferred

- PyPI trusted publishing remains intentionally deferred in the `Deferred: PyPI` milestone until the PyPI project and trusted publisher are configured.

## Validation

- `make check PYTHON=./.venv-dev/bin/python`
- `Release workflow on tag push`
