# AI News Open v1.2.55

## Highlights

- Added a release workflow failure summary so failed tag releases show checkout, setup, install, lint, coverage, build, checksum, SBOM, artifact upload, attestation, release publishing, and artifact-smoke dispatch outcomes in the GitHub step summary.
- Documented the recovery path for timed-out release tag pushes, including read-only remote tag verification and the GitHub Git API lightweight-tag fallback for this repository.
- Removed stale hard-coded patch release and milestone values from the roadmap; the roadmap now points readers to the release notes index for the latest patch release and to the active GitHub milestone for current work.
- Added release metadata coverage and release-checklist guidance so roadmap status, release notes, and release-maintenance docs stay aligned during future release prep.

## Deferred

- PyPI trusted publishing remains intentionally deferred in the `Deferred: PyPI` milestone until the PyPI project and trusted publisher are configured.

## Validation

- `make check PYTHON=./.venv-dev/bin/python`
- `Release workflow on tag push`
