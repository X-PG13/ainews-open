# AI News Open v1.2.71

## Highlights

- Added package-resource smoke coverage for web console assets so the installed package contract keeps `index.html`, `styles.css`, and `app.js` readable.
- Added compact web console diagnostics for CSS and JavaScript asset loading, including loaded paths, retry states, and inline fallback status in the preview-mode strip.
- Added English and Simplified Chinese README operator paths that connect first deploy, console walkthrough, troubleshooting, and release evidence docs.

## Deferred

- PyPI trusted publishing remains intentionally deferred in the `Deferred: PyPI` milestone until the project and trusted publisher are configured.

## Validation

- `make check PYTHON=./.venv-dev/bin/python`
- GitHub PR checks passed for the package asset smoke coverage, console asset diagnostics, and operator navigation updates before release prep.
