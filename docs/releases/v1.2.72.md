# AI News Open v1.2.72

## Highlights

- Added visible first-screen skeleton placeholders for web console metrics and operations, with warm loading feedback, reduced-motion behavior, and a paused waiting state for file-mode or failed refreshes.
- Added English and Simplified Chinese troubleshooting guidance for interpreting CSS and JavaScript asset diagnostics before changing code.
- Added a bilingual operator navigation map that connects setup, daily console operation, release verification, and recovery to their primary guides and completion signals.

## Deferred

- PyPI trusted publishing remains intentionally deferred in the `Deferred: PyPI` milestone until the project and trusted publisher are configured.

## Validation

- `make check PYTHON=./.venv-dev/bin/python`
- GitHub PR checks passed for the console asset diagnostics documentation, first-screen loading polish, and operator navigation map before release prep.
