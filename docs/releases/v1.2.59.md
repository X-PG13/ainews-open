# AI News Open v1.2.59

## Highlights

- Added explicit keyboard focus indicators for admin console form controls and check-label groups to improve accessibility.
- Added reduced-motion handling so console transition-heavy interactions minimize motion for users who prefer reduced motion.
- Improved release artifact verification instructions by replacing hard-coded version values with placeholders in English and Chinese docs.

## Deferred

- PyPI trusted publishing remains intentionally deferred in the `Deferred: PyPI` milestone until the PyPI project and trusted publisher are configured.

## Validation

- `make check PYTHON=./.venv-dev/bin/python`
- `Release workflow on tag push`
