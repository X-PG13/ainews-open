# AI News Open v1.2.54

## Highlights

- Added a GitHub-only artifact attestation verification path for release assets, including wheel, source archive, `sha256sums.txt`, and SBOM files.
- Clarified how release checksums, GitHub artifact attestations, and CycloneDX SBOMs serve different parts of a lightweight supply-chain review.
- Added release smoke failure summaries so maintainers can quickly tell whether a failure came from asset download, checksum validation, install, CLI startup, API health, or health payload validation.
- Added regression coverage for README release-maintenance entry points and the release smoke failure summary.

## Deferred

- PyPI trusted publishing remains intentionally deferred in the `Deferred: PyPI` milestone until the PyPI project and trusted publisher are configured.

## Validation

- `make check PYTHON=./.venv-dev/bin/python`
- `Release workflow on tag push`
