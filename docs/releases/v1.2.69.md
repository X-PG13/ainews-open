# AI News Open v1.2.69

## Highlights

- Hardened the web console static asset bootstrap so file-mode previews and served HTTP views retry the expected CSS and JavaScript locations before falling back to the inline static preview styles.
- Updated the GitHub Actions workflow checkout step to `actions/checkout@v7` through a human-authored maintenance PR, keeping Dependabot PRs as notifications only.
- Relaxed the development pytest upper bound to `<10.0.0` so future pytest 9 releases can be evaluated without another metadata-only range update.

## Deferred

- PyPI trusted publishing remains intentionally deferred in the `Deferred: PyPI` milestone until the project and trusted publisher are configured.

## Validation

- `make check PYTHON=./.venv-dev/bin/python`
- Temporary virtual environment install of `.[dev]` resolved `pytest 8.4.2` and ran `317 passed`.
- GitHub PR checks passed for the web console fallback hardening and both human-authored dependency maintenance PRs before release prep.
