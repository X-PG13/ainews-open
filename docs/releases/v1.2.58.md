# AI News Open v1.2.58

## Highlights

- Added regression coverage that enforces packaged console assets are present in both wheel and source archive outputs.
- Improved the zero-build admin console first-run experience with actionable empty states for ingest, extraction, digest review, publishing, source health, and publication history.
- Tightened the admin console narrow-view layout so token guidance, hero pills, workflow headers, and empty-state cards remain readable without horizontal overflow.
- Added a copy-pasteable operator console walkthrough that takes maintainers from a local checkout through serve, token setup, ingest, extraction, digest preview, frozen editor snapshots, and safe publication preview.

## Deferred

- PyPI trusted publishing remains intentionally deferred in the `Deferred: PyPI` milestone until the PyPI project and trusted publisher are configured.

## Validation

- `make check PYTHON=./.venv-dev/bin/python`
- `Release workflow on tag push`
