# AI News Open v1.2.70

## Highlights

- Added a web console preview checklist so maintainers and operators can diagnose plain-text-only rendering, missing CSS, local file-mode previews, and served HTTP console views with a repeatable process.
- Added a post-publish release evidence checklist covering tag, GitHub Release assets, workflow status, release smoke results, and milestone closeout evidence.
- Added a preview-mode status strip to the web console hero so operators can tell whether they are viewing a static file preview, an HTTP view waiting for backend data, or a connected backend session.

## Deferred

- PyPI trusted publishing remains intentionally deferred in the `Deferred: PyPI` milestone until the project and trusted publisher are configured.

## Validation

- `make check PYTHON=./.venv-dev/bin/python`
- GitHub PR checks passed for the web console preview checklist, release evidence checklist, and preview-mode status strip changes before release prep.
