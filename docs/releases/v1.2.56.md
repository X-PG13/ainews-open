# AI News Open v1.2.56

## Highlights

- Added quick reference tables to the release recovery notes so maintainers can jump directly to the right verification command after interrupted PR merge, tag push, workflow, release asset, and issue or milestone update operations.
- Improved the release workflow fallback notes for missing release documentation. Instead of publishing a bare placeholder, the fallback now names the missing `docs/releases/vX.Y.Z.md` file and tells maintainers to add it before final release notes are published.
- Expanded the release closeout checklist so roadmap status, release notes, milestone closure, deferred work movement, and next milestone creation happen in a consistent order.
- Added release metadata coverage for the fallback release notes contract and the milestone closeout sequence.

## Deferred

- PyPI trusted publishing remains intentionally deferred in the `Deferred: PyPI` milestone until the PyPI project and trusted publisher are configured.

## Validation

- `make check PYTHON=./.venv-dev/bin/python`
- `Release workflow on tag push`
